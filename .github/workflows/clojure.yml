name: Clojure CI

on:
  push:
    branches: [ "DOXIA-569" ]
  pull_request:
    branches: [ "DOXIA-569" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - name: Install dependencies
      run: lein deps
    - name: Run tests
      run: lein test
